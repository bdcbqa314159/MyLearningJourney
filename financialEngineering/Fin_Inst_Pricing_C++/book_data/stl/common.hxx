// common.hxx
//
// My common file for all STL stuff

#ifndef COMMON_HXX
#define COMMON_HXX

#include <iterator>		// All kinds of iterators
#include <set>			// Includes set and multiset
#include <iostream>
#include <algorithm>
#include <map>			// Includes map and multimap
#include <list>
#include <vector>
#include <functional>	// Function objects
#include <numeric>		// For numerical algorithms (e.g. inner_product)
#include <stddef.h>		// For ptrdiff_t
#include <queue>		// For queue and priority_queue
#include <deque>
#include <cmath>		// Commonly occurring maths functions
#include <stdlib.h>		// for rand, for example
#include <memory>		// Class allocator
#include <stack>

//typedef size_t size_type;

using namespace std;	// Save us from typing std::  every time we use something from the standard library


#endif
